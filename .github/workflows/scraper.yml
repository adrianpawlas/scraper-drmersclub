name: Drmers Club Scraper

on:
  schedule:
    - cron: '0 5 * * 2,6'
  workflow_dispatch:
    inputs:
      limit:
        description: 'Product limit (leave empty for all)'
        required: false
        type: string
      skip-embedding:
        description: 'Skip embedding generation'
        required: false
        type: boolean
        default: false

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: '3.14'

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq libsentencepiece-dev

      - name: Cache Python packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
          pip install -r requirements.txt

      - name: Cache HuggingFace model
        uses: actions/cache@v4
        with:
          path: ~/.cache/huggingface
          key: ${{ runner.os }}-huggingface-siglip
          restore-keys: |
            ${{ runner.os }}-huggingface-

      - name: Run scraper
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          HF_HOME: /home/runner/.cache/huggingface
        run: |
          ARGS=""
          if [ -n "${{ github.event.inputs.limit }}" ]; then
            ARGS="$ARGS --limit ${{ github.event.inputs.limit }}"
          fi
          if [ "${{ github.event.inputs.skip-embedding }}" = "true" ]; then
            ARGS="$ARGS --skip-embedding"
          fi

          # Retry logic: up to 2 attempts on failure
          for i in 1 2; do
            echo "--- Run attempt $i ---"
            python main.py $ARGS && EXIT_CODE=0 && break || EXIT_CODE=$?
            if [ $i -eq 1 ]; then
              echo "Attempt $i failed (exit $EXIT_CODE). Retrying in 30s..."
              sleep 30
            fi
          done
          exit $EXIT_CODE
